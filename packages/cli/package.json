{
  "name": "@inox-edot/cli",
  "version": "0.1.0",
  "description": "EDOT React Native CLI — source map upload and SDK tooling",
  "license": "MIT",
  "bin": {
    "edot": "./lib/index.js"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "oxlint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.4.0"
  }
}
