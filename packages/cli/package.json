{
  "name": "@inoxth/react-native-edot-cli",
  "version": "0.1.0",
  "private": true,
  "description": "EDOT React Native CLI — source map upload and SDK tooling",
  "license": "MIT",
  "bin": {
    "edot": "./lib/index.js"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf lib tsconfig.tsbuildinfo",
    "lint": "oxlint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^6.1.3",
    "typescript": "~5.8.3"
  }
}
