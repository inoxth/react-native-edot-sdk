---
"@inoxth/react-native-edot-navigation": minor
---

Release in lockstep with `@inoxth/react-native-edot-sdk` **0.2.0** (the iOS 15.6 / RN 0.81 native-agent downgrade). No API changes in this package; the bump keeps the suite on a single version line and pins it to the trimmed native-module contract in `@inoxth/react-native-edot-shared` 0.2.0.
